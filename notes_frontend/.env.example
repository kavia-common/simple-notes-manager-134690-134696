# PUBLIC_INTERFACE
# Backend API base URL for notes service.
# If not provided, the app will attempt to use same-origin relative paths
# and will fallback to a local in-browser mock store when unreachable.
REACT_APP_NOTES_API_URL=
